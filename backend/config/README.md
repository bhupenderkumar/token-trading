# Configuration Files

This folder is for environment variables, secrets, and config templates.

- Add `settings_template.py` for config structure
- Add `.env.example` for environment variable examples
